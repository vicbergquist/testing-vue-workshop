test('fetchItems commits setLoading when called')

test(
  'fetchItems commits items returned by api method and sets loading to false'
)

test('fetchItems commits error and sets loading to false if api method rejects')
