describe('ItemView.vue', () => {
  test('dispatches fetchItem with id')

  test('renders a comment for each comment')

  test('renders spinner if item has comments')

  test('hides spinner when comments are loaded')
})
